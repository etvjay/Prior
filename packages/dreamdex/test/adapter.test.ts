import { describe, expect, it } from "vitest";
import { DreamDexAdapter, _internals } from "../src/index.js";
describe("DreamDexAdapter read path",()=>{it("uses configured indexer and rejects HTTP errors",async()=>{const original=globalThis.fetch;let requested="";globalThis.fetch=(async(input)=>{requested=String(input);return new Response("no",{status:503});}) as typeof fetch;try{const adapter=new DreamDexAdapter({rpcUrl:"http://rpc.invalid",indexerUrl:"http://indexer.test/graphql"});await expect(adapter.listRecentMarkets()).rejects.toThrow("indexer request failed: 503");expect(requested).toBe("http://indexer.test/graphql");}finally{globalThis.fetch=original;}});it("rejects unsupported asset and cadence mappings",()=>{expect(()=>_internals.classify("SOL",60)).toThrow(/unsupported market class/);});});
